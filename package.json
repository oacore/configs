{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.4"
  },
  "scripts": {
    "version": "lerna version --git-tag-version=false  --force-publish",
    "publish": "lerna publish"
  },
  "dependencies": {
    "eslint": "^6.6.0"
  }
}
