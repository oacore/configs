{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-prettier": "^3.1.1",
    "lerna": "^3.18.4",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.19.1"
  },
  "scripts": {
    "install:eslint-config-base": "cd packages/eslint-config-base && npm install",
    "install:eslint-config-react": "cd packages/eslint-config-react && npm install",
    "install:prettier-config": "cd packages/prettier-config && npm install",
    "postinstall": "npm-run-all install:*",
    "version": "lerna changed && lerna version",
    "publish": "lerna changed && lerna publish from-package",
    "lint": "eslint --ignore-path .gitignore --ext .js,.jsx .",
    "format": "eslint --ignore-path .gitignore --ext .js,.jsx --fix ."
  }
}
